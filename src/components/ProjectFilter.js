import React from "react";

const ProjectFilter = () => {
  return (
    <div>
      <p>this is a filter</p>
      {/* Featured Projects */}
      <button>Featured</button>
    </div>
  );
};

export default ProjectFilter;
